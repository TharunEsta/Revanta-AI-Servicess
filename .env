# RevOps local SQLite database
# Prisma will create revops.db automatically via migrations.
DATABASE_URL="file:./dev.db"

# Existing auth envs (used by current login route). Set these for local testing.
# REVOPS_USERNAME="SalesAI"
# REVOPS_PASSWORD_HASH="<bcrypt hash>"

